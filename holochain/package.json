{
  "name": "@holochain-open-dev/profiles-dev",
  "private": true,
  "workspaces": [
    "ui",
    "tests"
  ],
  "scripts": {
    "start": "npm run pack:happ && npm run start:app",
    "network": "npm run pack:happ && concurrently-repeat \"npm run start:app\"",
    "start:app": "cross-env HC_PORT=$(port) concurrently \"npm run playground\" \"npm run start:happ\" \"yarn --cwd ../ui ioen\"",
    "build:profiles": "yarn --cwd ./profiles build:dna",
    "build:energy_monitor": "yarn --cwd ./energy_monitor build:dna",
    "pack:happ": "npm run build:profiles && npm run build:energy_monitor && hc app pack ./workdir",
    "start:happ": "npm run pack:happ && RUST_LOG=warn hc s generate ./workdir/ioen-app.happ --run=$HC_PORT -a ioen-app network mdns",
    "start:picogridhapp": "npm run pack:happ && RUST_LOG=warn hc s generate ./workdir/ioen-app.happ --run=$(port) -a ioen-app network mdns",
    "playground": "run-singleton \"holochain-playground\""
  },
  "devDependencies": {
    "concurrently": "^6.2.1",
    "concurrently-repeat": "^0.0.1",
    "cross-env": "^7.0.3",
    "new-port-cli": "^1.0.0",
    "run-singleton-cli": "^0.0.5"
  },
  "type": "module",
  "dependencies": {
    "@holochain-playground/cli": "^0.0.8"
  }
}